language: python
sudo: required
dist: trusty
group: edge
python:
  - "3.5"
before_install:
  - sudo apt-get -qq update
install:
  - sudo apt-get update -y
  - sudo apt-get install build-essential curl file git python-setuptools ruby liburi-escape-xs-perl emboss liburi-perl libsm6 libxrender1 libfontconfig1
  - wget https://repo.continuum.io/miniconda/Miniconda3-latest-Linux-x86_64.sh -O miniconda.sh && bash miniconda.sh -b -p $HOME/miniconda
  - export PATH="$HOME/miniconda/bin:$PATH"
  - hash -r
  - conda config --set always_yes yes --set changeps1 no
  - conda update -q conda
  - conda info -a
  - conda create -q -n test numpy scipy pandas matplotlib pytest pytest-cov coverage seaborn
  - source activate test
  - pip install shmlast --no-deps
  - pip install codecov
  - git clone https://github.com/Linuxbrew/brew.git $HOME/.linuxbrew
  - export PATH="$HOME/.linuxbrew/bin:$PATH"
  - brew update
  - brew install homebrew/science/last homebrew/science/transdecoder parallel homebrew/science/infernal homebrew/science/hmmer
  - brew install --build-from-source --without-test homebrew/science/blast
  - pushd $HOME && git clone https://gitlab.com/ezlab/busco && cd busco && python setup.py install && popd
  - export PATH="$HOME/busco/scripts:$PATH"
script: make ci-test
